{% extends 'base.html.twig' %}

{% block body %}
    <h1>Film edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
        <input type="submit" value="Save" class="custom-btn btn-success btn"/>
    {{ form_end(edit_form) }}

    {{ form_start(delete_form) }}
    <input type="submit" value="Delete" class="custom-btn btn-danger btn">
    {{ form_end(delete_form) }}

    <a href="{{ path('film_index') }}" class="custom-btn btn-primary btn">Back to the list</a>

{% endblock %}
