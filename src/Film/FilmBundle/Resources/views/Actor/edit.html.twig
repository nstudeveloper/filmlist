{% extends 'base.html.twig' %}

{% block body %}
    <h1>Actor edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
        <input type="submit" value="Save" class="btn btn-primary custom-btn"/>
    {{ form_end(edit_form) }}
    <a href="{{ path('actor_index') }}" class="btn btn-primary custom-btn">Back to the list</a>

    {{ form_start(delete_form) }}
        <input type="submit" value="Delete" class="btn btn-danger custom-btn">
    {{ form_end(delete_form) }}

{% endblock %}
